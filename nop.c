#include "monty.h"

/**
 * nop - multiply top 2 numbers
 * @head: pointer to the stack
 * @line_num: current line number
 */
void nop(stack_t **head, unsigned int line_num)
{
	(void) head;
	(void) line_num;
}
